# UI Staging Area

This folder is the clean UI track for Wallchain Select.

Use it for:

- handoff-ready pages
- cleaned-up block implementations
- UI-only assets and structure

Do not put wireframe history or demo-only experimentation here.
The wireframe track lives in `v1.x`.

